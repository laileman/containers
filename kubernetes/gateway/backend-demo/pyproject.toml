[project]
name = "backend-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116.2",
    "pyyaml>=6.0.2",
    "uvicorn>=0.35.0",
]
