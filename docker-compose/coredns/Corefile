.:53 {
        errors
        log
        health {
           lameduck 15s
        }
        ready
        prometheus :9153
        forward . /etc/resolv.conf {
              prefer_udp
        }
        
        cache 30
        loop
        reload
        loadbalance
}
local.:53 {
        file /local.zone
        log
        errors
        reload
        cache 30
}
cluster.:53 {
        file /cluster.zone
        log
        errors
        reload
        cache 30
}
nginx.local.:53 {
        file /nginx.local.zone
        log
        errors
        reload
        cache 30
}

